# LinkedList
An implementation of a linked list that stores objects with properties
The LinkedList class contains several methods that allow for traversal of the list, insertion and deletion of objects
whether that be at the front of the list or the back.
